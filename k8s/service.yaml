apiVersion: v1
kind: Service
metadata:
  name: auth-framework
  namespace: auth-framework
  labels:
    app.kubernetes.io/name: auth-framework
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auth-framework
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
